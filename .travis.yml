language: ruby
rvm:
 - 2.2
 - jruby
 - rbx-2
# uncomment and edit the following line if your project needs to run something other than `rake`:
# script: bundle exec rspec specscript:
script:
- bundle exec rake db:migrate RAILS_ENV=test
- bundle exec rake test
